{
  "buildCommand": "rm -rf .metro node_modules/.cache node_modules/react-native-css-interop/.cache && pnpm install && npx expo export --platform web --clear",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null
}
